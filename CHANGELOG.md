# 更新记录

以下内容整理自各版本发布时的 README 说明，保留当时的措辞与验证范围。最新版本在前。

## v0.3.0 Android 端培养方案与完成度

Android 应用版本记为 v0.3.0（`versionCode` 2）。新增"培养方案"，入口在"我的"：离线内置教务部公开的 2021/2023/2024/2025 版共 599 份方案（构建时由 Gradle 从仓库根 `data/curriculum/` 同步进 assets，不在 `android/` 下复制副本），附 35 个单元测试。

- 匹配规则逐条对齐桌面端 `src/lib/curriculum.ts`：课程名规范化、六级匹配瀑布（手动归类 → 精确名 → 变体基名 → 反向变体 → 公共课关键词 → 课程类别 → 待确认）、两层学分系列与父级汇总、按最早学期推断年级并回退到不晚于该年的最新版、按专业必修课重合度排候选。候选排序多一层优化：门户「单位」命中时先限定本院系，本院系无重合则退回全校，零重合不猜。
- 界面按"每个系列一页 + 左右滑动"组织，顶部为当前系列名与滑动指示条；圆环和直线进度条都按桌面端配色画出"在修"淡色段，每页先列"在修课程 N 门"再列"已修课程 N 门"，列出该系列及其子系列的课程，条目右侧直接写拿到多少学分。课程卡（已识别的、待确认的、不计入的都一样）整张可点，弹同一个编辑框改学分与归类，改一门课后停留在原页面、不跳回第一页；条目内不再放一排按钮。方案原文只给教务部整卷入口，不在应用内抽页。
- 与桌面端规则不同的六处，每处都有原文或数据依据：
  - 有些方案（如 2025 物理学院-物理学）不在要求树里写大类总额、也整类漏掉某个子系列，手机端回退到课程组的 `min/max`，并在"恰好一类没写学分要求"时用大类余额补出下限。
  - 父类不给总额、下面按方向分列的那一类（物理学院的专业核心课分了五个方向，18～24 学分不等），"选择方案"页新增细分方向选择：选定后这一类按该方向的学分计、大类总额按各子系列求和重算，只有其他方向独有的课不再算进这一类。
  - 在修课程的学分不从方案回填——教学网课程列表本就没有学分字段（桌面端 `Course` 类型也只有 `id/name/semester/current`），而方案 PDF 清洗时物理学院 145 门课有 61 门把学分列吞进了课程名，所以改由点课程卡在编辑框里手工填写，填了才按桌面端配色算进淡色弧与缺口。
  - 手填的 0 分是有效值，不像桌面端那样把 0 当成"没填"：确实存在 0 学分的课（习题课、部分特殊思政课），原先填 0 保存后会退回"待填学分"。同时手填过就以手填为准，桌面端只在源数据缺学分时才取手填值，所以那里改了不会生效；清空该栏即撤销手填、回到源数据。
  - 课程名含"习题"的在修课程不计入：教学网把习题课与正课并排列在在修清单里，两门都算会重复计数，而习题课本身不计学分。
  - 英语分级改按《北京大学大学英语课程培养方案（2025 年 6 月修订）》"二、修读要求"的原文口径实现：分级就是"公共必修课"里大学英语的学分要求（Y/A/B/C/C+ = 8/8/6/4/2，免修按第 3 条获 2 分，英语专业与留学生按第 1 条豁免）。方案把英语单列成一类的直接定住；折在"公共必修课"里的，按"该类区间跨度恰好等于弹性 8−2=6"唯一认定（全库 144 份属此类，候选不唯一的 0 份），然后大类按子系列求和、毕业总学分按大类求和，两者都只在结果落在方案自述区间内才采用。桌面端的做法是"不足 8 学分的差额补进通识教育课、大类顶到上限"，原文并无必须补修的表述，所以这里不再补差额、也不再一律顶格：生命科学学院-生物科学选 B 级得到公共基础 49 学分（而非 51），物理学院-物理学选 B 级 + 物理学方向得到公共基础 49、专业必修 76、毕业总学分 150，全库分级生效面从 276 份增至 412 份。
- 本版不改 `data/curriculum`。排查中发现两处上游数据缺陷：226/599 份方案把"— 大学英语 2～8 —"这类无课号子项行当成续行丢进了 `notes`；而用未改动的解析器重跑全部 8 卷只得 592 份、55 个方案 id 消失（`build.mjs` 的 `inferTitles` 参照集边读边写 `data/curriculum`，构建不可复现），因此不把这些抖动混进本分支，留作上游处理。

## v0.2.0 新增 Android 客户端

Android 应用版本记为 v0.2.0（`android/`，Kotlin + Jetpack Compose + MVVM + Hilt，minSdk 26），协议层移植自本仓库 Rust 代码。功能覆盖：账号密码登录（凭证本机加密保存、会话失效自动重登）、今日聚合、教学网课程/通知/作业/资料/教师成绩、作业汇总与详情、树洞成绩单与 GPA、学校/部门/本院/教务部/图书馆通知、空闲教室、校历 PDF、校园卡余额与流水。GitHub Actions 工作流在推送 Android 分支时构建调试 APK 并上传产物；发布 Release 时附带 APK。详情见 `android/README.md`。

- 作业提交：沿用桌面版规则，单个文件、25 MB 上限、明确确认后上传；上传后重新读取提交记录并下载回执做 SHA-256 核对，不一致或读不到回执时提示人工核对，不自动重发。课程详情页新增"作业"标签，作业详情提供提交入口。
- 成绩双口径：GPA 与加权平均分可按"全部课程"或"专业必修/限选"分别计算；学校返回的官方 GPA 只用于全部课程口径并标注，其余一律标注为本地计算。部分专业课在学校系统里被标成"任选"，因此专业口径支持逐门手动增删，记录只存本机。
- 校园卡修正：余额改为电子账户口径（原先把 `accinfo` 子账户重复相加，导致翻倍）；月度统计接口返回的是分，原先按元显示，放大了一百倍；"本月支出"改用消费类流水分类统计求和，"本月充值"单列。
- 作业状态修正：提交记录此前只读"尝试历史链接"，只提交过一次时学校不渲染该列表，导致显示"暂无提交记录"；分数在 `input` 的 `value` 属性上，原先取元素文本恒为空；"待交"筛选此前只看截止时间，现已结合提交记录与成绩中心判定，并新增"已提交"筛选与分数显示。
- 院系识别与本院通知：新增校内门户服务会话（IAAA appID `portal2017` → `ssoLogin.do`），读取 `account/getBasicInfo.do` 的「单位」确定所在院系；已适配物理学院、计算机学院官网通知页（各校 CMS 不一，按"详情链接 + 邻近日期"通用提取，正文取最大候选容器），信科沿用原适配器；未适配的院系回退到门户部门通知按院系列过滤（该接口不支持服务端筛选，翻最近若干页本地匹配）。通知页原"信科"标签改为"本院"，设置页可查看识别结果并手动指定院系。
- 应用图标改用桌面版的"未"字图标（直接采用 `src-tauri/icons/android` 下已生成的各密度资源）；校历 PDF 页支持双指缩放与拖动，翻页自动复位。
- 除上述作业提交外，不含视频回放、树洞互动、校园卡充值等写操作。

## v0.1.0 首个公开版本：开源、培养方案与应用内更新

开源前的内部迭代（下文 v0.3–v0.6）版本号沿用当时编号，不曾公开发布；公开版本从 v0.1.0 重新计数。

- 设置页新增"版本与更新"：检查 GitHub Releases 上的新版本，应用内下载、校验签名并安装，重启生效。更新地址写死在应用配置里，公钥内置；不收集使用数据。发布流程见 `.github/workflows/release.yml`：推送 `v*` 标签生成草稿 Release，维护者发布后客户端可见。
- 扫码登录窗口打开即自动获取二维码，过期或失败时才需要点"重新获取"。
- 培养方案页圆环选中态改为只描边，不再整卡变色。界面分隔线与边框加深。
- 设置页重排为四组（账号、年级与专业、本机数据、关于），每项一行：左侧名称与一句说明，右侧一个控件；说明文字不再单独成段，次要操作收进折叠项。设置页可更改下载与资料的保存目录；已下载的文件不会自动搬移。次要按钮悬停时显示边框，按下有反馈。

- 仓库按 MIT 开源：新增 LICENSE、CONTRIBUTING、SECURITY、THIRD-PARTY-NOTICES；README 改为面向新用户，末尾致谢列出借鉴过的项目；版本说明迁到本文件。删除了含个人数据的界面审计截图。
- 新增"培养方案"页：教务部公开的 2021、2023、2024、2025 版培养方案 PDF 经 `scripts/curriculum/` 清洗为 `data/curriculum/` 下 599 份结构化方案（约七成带解析警告，逐条列在方案页的"数据说明"里，可通过 `scripts/curriculum/overrides` 修正）。首次使用时按成绩与课程学期推断入学年份，按专业必修课重合度推荐专业；重合门数打平时明确提示。年级、版本、院系、专业、双学位方案均可改，之后在设置页修改。
- 完成度以圆环呈现：毕业总学分与各大类各一个环，环内为已获 / 要求，在修为浅色弧；点某个环在下方展开该类的学分系列、进度条与课程列表。课程名匹配不上的进"待确认"，用户手动归类或标为不计入，结果只存本机 Preferences。非表格化的方案例外原文照抄展示，不假装算清。
- 大学英语按分级计算：在资料里选 Y / A / B / C / C+ 级或免修（分别 8、8、6、4、2、2 学分，依据 2025 版《北京大学大学英语课程培养方案》），英语系列按所选学分固定，不足 8 学分的差额计入通识教育课要求。
- 每份方案可查看原文：应用从教务部整卷 PDF 中抽出该专业所在的几页，应用内预览，并保存到 `~/Downloads/OnePKU/培养方案/`；整卷只下载一次，缓存在本机。前端只能传卷 id 与页码，下载地址来自仓库内的卷目清单。
- 课程详情新增评课入口，深链拼好课、非官方课程测评、PKUHUB，不接入其评分数据。
- README 写明无公证应用在 macOS 14、15 及"已损坏"三种情形下的打开办法。会话互踢与浏览器登录态导入暂未处理。

## v0.6.0 本地回放与原文窗口

- 课程 → 课程回放 → 播放：应用内边下边播，无需等待整节 MP4。支持进度条、前后跳转、0.75–2 倍速和全屏；按账号与回放保存观看位置。
- 播放时缓存整节，可暂停/继续缓存；关闭播放器或退出应用停止后台获取，已完成分片保留，重新打开复用。完整缓存可离线播放；部分缓存遇到未下载片段仍需联网。播放器内可清除此回放缓存。
- 教学网原始回放索引与正常授权链路继续复用 PKU CLI。签名地址、Cookie、AES 密钥只存在后端内存，播放器收到本机地址。无效/截断片段不写入缓存；单节缓存上限 8 GB。
- “下载 MP4”保留为独立文件归档功能，当前不复用播放器缓存；播放器缓存无需 ffmpeg，MP4 归档仍需 ffmpeg。
- 公共通知点击标题直接打开 OnePKU 原文窗口，保留原站图片、附件、登录及交互；课程通知继续显示教学网已返回的文本。菜单“网页”支持后退、前进、刷新。需要认证的站点须在窗口正常登录，窗口 Cookie 与后端查询会话独立。
- 图书馆活动作为可选通知源接入，本轮读取 48 项真实活动。列表索引区分更新日期和活动日期，重复使用的文章按每场活动保存已读状态。

当前界面调整：课程与作业详情使用主区域页面和 hash 历史导航，课程中不再重复提供作业；独立智云课堂实录 GUI 已撤下，底层只读接口保留。

课程资料支持从系统文件选择器多选添加微信已保存附件、个人笔记等文件，复制到现有课程目录。同内容复用，同名不同内容保留新版本；原文件保留。点击本机资料可打开，删除会经行内确认移到系统废纸篓，可从废纸篓拖出恢复，不影响教学网原件。教学网下载保留，可与本机资料一起检索。提醒页面与后台系统通知检查已移除。

课程通知按当前/历史课程读取，与全局通知共享已读状态。点击进入对应教学网原文，图片和链接由学校页面呈现；原生窗口只接收目标教学网域名匹配的会话 Cookie，凭证不进入前端。教参不单独新增入口。提交历史最多显示 30 次，个别记录读取失败单独标注；缺失分数保留为未评分。

### 回放字幕

回放播放器支持本机分段生成字幕、导入 SRT / VTT、自动记住字幕和时间偏移，普通播放与全屏均可显示。每段完成后立即显示并保存，停止或重开应用后可继续生成剩余部分。识别模型通过独立适配层替换；当前本机使用 Belle Whisper 中文模型和人声检测。设置和接口见 [字幕说明](docs/SUBTITLES.md)。

## v0.5.0 学习工作区与场地入口

- 教学网课程详情聚合资料、回放、作业和教师成绩；独立作业页支持本学期汇总和历史课程筛选。
- 智云独立目录按日期/课程名搜索，再查看全部课次。默认筛选已有回放；已下架和未开放状态不能变成播放授权。尚未获得一个“教学网已下架、独立系统仍开放”的具体课程案例，不把此说法当作保证。
- 课件批量队列上限 200 个任务；录像通过学校授权播放链接下载 HLS，4 个分段并行、按原顺序合并，单条上限 8 GiB。需要本机 ffmpeg；此 Mac 已具备。结束后按学期/课程归档，附来源记录。退出应用会中断队列，暂不支持断点续传。
- 场地预约按用户决定暂缓上线：主导航、页面路由和对应账号入口已撤下。保留底层代码供学校开放后继续验证。
- CLI 的三个查询命令已整理为 `contributions/pkucli-course-read.patch`，基于当前上游提交，尚未推送或发起 PR；已安装的全局 PKU CLI 未替换。

不可用服务与新接口候选见 [服务探查记录](docs/research/SERVICE-EXPLORATION.md)。

## v0.4.0 写入口与能力取舍

作业详情提供首次单文件提交：选择文件（25 MB 上限）→ 核对课程、作业和附件 → 明确提交。只有学校回执附件与本地副本 SHA-256 一致才确认成功；异常进入待核对记录，不自动重发。暂不处理多次尝试、追加和富文本提交，这些打开原作业办理。当前 Lab 0 教师说明无需提交文件，界面正确不显示上传按钮。

校园卡充值/支付、树洞互动、选退课与预约使用学校原有页面。具体判据和阶段决策见 [能力吸收规则](docs/ADOPTION.md)。写流程已进行模拟请求、持久记录和原生界面验证；未替用户向学校提交真实作业，真实文件发送仍须用户点击最终按钮。

## v0.3.0 实际覆盖

已用当前账号验证 28 门当前/历史课程、可视计算 9 月 7 日回放入口、19 条成绩及校园卡月度三类统计。成绩源未提供 GPA 时显示缺失，不自行计算；考试源当前返回不可用，保留独立重试与校内门户入口，不能宣称已取得考试安排。回放目前调用教学网页播放，没有实现流媒体离线下载。单个课件真实下载和重复下载已核验，源文件保留且 SHA-256 与元数据一致。

GUI 的取舍：以集中查看和整理为主；树洞的搜索/线索检索保留在底层和 CLI，未创建 MCP 接入。上游写能力与 OnePKU 当前边界见 [能力盘点](docs/research/CAPABILITIES.md)。
