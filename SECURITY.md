# 安全说明

## 数据存放位置

| 内容                     | 位置                               | 说明                                                                                  |
| ------------------------ | ---------------------------------- | ------------------------------------------------------------------------------------- |
| 登录凭证（Cookie、令牌） | `~/.config/info/<服务>/`           | 与 PKU CLI 共用，文件权限 0600，不进入前端                                            |
| 资源缓存                 | 应用缓存目录 `resources-v1.json`   | 按账号指纹隔离，最长 30 天，可在设置中清除                                            |
| 偏好、已读、订阅         | 应用配置目录 `preferences.json` 等 | 本机文件                                                                              |
| 下载的课件与回放         | 默认 `~/Downloads/OnePKU/`         | 可在设置中改为任意本机文件夹（经系统选择框，前端不能传路径）；附来源与 SHA-256 元数据 |
| 作业提交暂存             | 应用私有目录                       | 未发送文件在取消、过期或重启时清理                                                    |

账号密码登录默认勾选「记住密码」，可取消。只有服务登录成功后，才将账号密码保存在操作系统钥匙串（macOS Keychain / Windows Credential Manager / Linux Secret Service），不写入 JSON、资源缓存、浏览器存储或日志。使用 OnePKU 独立的条目，不覆盖 PKU CLI 的共享密码。设置中的「忘记密码」会删除这些条目，当前会话继续可用。保存失败明确提示，不退回明文存储。

前端输入的密码只经独立原生 IPC 传到 Rust，再通过学校 IAAA 的 RSA 公钥加密提交；已保存的密码和会话令牌不返回前端。浏览器预览不接受密码登录。动态口令不保存，短信和扫码确认仍由用户完成。

自动重登只在开启「保持登录」且普通读取明确遇到认证失效时触发；保存的密码必须仍绑定当前服务的会话指纹，扫码或其他账号切换后不使用旧密码恢复账号。每个失效会话仅尝试一次，失败交回手动登录；近期重登节流。成功后最多重读一次，绝不重放作业提交、下载控制、短信或其他写操作。

## 应用不会做的事

- 不向学校以外的服务器发送任何账号数据；没有遥测、没有崩溃上报。
- 不自动执行支付、发帖、选退课、预约。作业提交需用户逐步确认，且只在学校回执与本地副本哈希一致时才标记成功。
- 后台保活做轻量会话检查；已记住密码的认证恢复不发送短信、不重试写操作。
- 内置原文窗口只把与目标学校域名匹配的会话 Cookie 注入原生 WebView，JavaScript 拿不到凭证。

## 应用内更新

- 更新只从 `tauri.conf.json` 中写死的地址（本仓库 GitHub Releases 的 `latest.json`）读取，前端不能改地址。
- 更新包用 minisign 签名，公钥内置在应用里，签名不符则拒绝安装。私钥只在维护者手里与 GitHub Actions 密钥中，不在仓库内。
- 检查更新由用户手动触发，不自动检查、不上报版本或设备信息。

## 报告漏洞

请不要在公开 issue 里贴出可复现凭证泄露的细节。通过 GitHub 私密漏洞报告（Security Advisories）联系维护者，或先发一个只写"发现安全问题，请私聊"的 issue，维护者会跟进。

收到报告后会先确认影响范围，再修复并在 CHANGELOG 中说明。涉及上游 PKU CLI 的问题会同时通知上游。
